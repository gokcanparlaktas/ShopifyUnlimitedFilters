export const metadata = {
  title: "Unlimited Filters",
  description: "Configure Shopify product metafield filters",
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body style={{ margin: 0 }}>{children}</body>
    </html>
  )
}
